{
  "name": "blackwood-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:wasm": "cargo build --release --locked -p blackwood-wasm --target wasm32-unknown-unknown --manifest-path ../../Cargo.toml && mkdir -p public && cp ../../target/wasm32-unknown-unknown/release/blackwood_wasm.wasm public/blackwood.wasm",
    "build:static": "npm run build:wasm && npm run build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "svelte": "^5.56.10",
    "vite": "^6.4.3"
  }
}
